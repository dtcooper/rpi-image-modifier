import sys
import os

print(f"{sys.version=}")

print(f"{os.environ['ARG_DOWNLOADED_IMAGE_WORKDIR']=}")
print(f"{os.environ['ARG_IMAGE_PATH']=}")
print(f"{os.environ['ARG_FILES']=}")
print(f"{os.environ['ARG_SCRIPT_PATH']=}")
print(f"{os.environ['ARG_RUN']=}")
